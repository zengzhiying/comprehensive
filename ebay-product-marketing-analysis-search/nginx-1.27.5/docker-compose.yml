services:
  nginx:
    image: nginx:1.27.5
    restart: always
    volumes:
     - ./nginx.conf:/etc/nginx/nginx.conf
     - ./conf.d:/etc/nginx/conf.d
     - /var/log/nginx:/var/log/nginx
     - ./html:/var/www/html
    ports:
     - "80:80"

